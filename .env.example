# Optional: public media origin with no trailing slash.
# Production defaults to the Guangzhou Tencent COS bucket. Set this only to
# override that origin; local development keeps serving files from /public.
PUBLIC_MEDIA_BASE_URL=
